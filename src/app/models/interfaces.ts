export interface User {
  username: string;
  name: string;
  email: string;
}

export interface Pedido {
  tipo: string;
  origen: string;
  peso: number;
  maduracion: string;
  cantidad: number;
}

